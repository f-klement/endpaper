# Frontend

React 19 + TypeScript, built by Vite 8, styled with Tailwind 4, installed and run with
Bun. Server state is React Query; the API client and its hooks are generated by Orval from
the backend's OpenAPI schema.

## The colocation rule

Everything lives as close as possible to the thing that uses it, and moves up only when it
is genuinely shared. Three levels:

1. **Used by one page** → inside that page's folder.
2. **Shared by sibling pages** → hoisted to the layer above them, `pages/components/`.
3. **General, reusable and domain-free** → `src/components/`, and nothing else goes there.

Worked examples: `BookCard` is used by Home's grid and by the appearance picker's preview,
so it moved up to `pages/components/BookCard.tsx`. `TagPicker` is used by Home's filter panel,
ScanPage's tag step and BookDetail's tag editor, so it is
`pages/components/TagPicker.tsx`. `Spinner` and `EmptyState` mention no domain concept at
all, so they are in `src/components/`.

The bar for `src/components/` is deliberately high: if a component mentions a book, a loan
or a tag, it does not belong there.

## Layout

```
src/
├── app/                    the shell: routing, providers, top bar
│   ├── App.tsx             session gate: signed out, every path is the login page
│   ├── providers.tsx       QueryClientProvider + ErrorBoundary
│   ├── routes.tsx          the signed-in route table
│   ├── hooks.ts            useExportLibrary
│   └── components/NavBar.tsx
├── api/
│   ├── mutator.ts          the ONLY module that calls fetch
│   ├── query-client.ts     cache and retry defaults
│   └── generated/          Orval output, committed, never hand-edited
├── components/             general dumb components only
├── i18n/                    en.ts · de.ts · index.tsx (provider + useTranslation)
├── lib/                     isbn.ts, goodreads.ts: pure functions, no React
├── pages/
│   ├── components/         shared by several pages (TagPicker, BookCard)
│   ├── hooks.ts            cross-page hooks (useSession)
│   ├── types.ts            cross-page view types, tag grouping and style tables
│   ├── AppearancePage/     index.ts · AppearancePage.tsx · hooks.ts · components/
│   ├── Home/               index.ts · Home.tsx · hooks.ts · types.ts · components/
│   ├── BookDetail/         index.ts · BookDetail.tsx · hooks.ts · components/
│   ├── ScanPage/           index.ts · ScanPage.tsx · hooks.ts · types.ts · components/
│   ├── LoansPage/          index.ts · LoansPage.tsx · hooks.ts · components/
│   ├── LoginPage/          index.ts · LoginPage.tsx · hooks.ts · components/
│   ├── SeriesPage/         index.ts · SeriesPage.tsx · hooks.ts · components/
│   ├── AuthorsPage/        index.ts · AuthorsPage.tsx · hooks.ts · components/
│   ├── CollectionsPage/    index.ts · CollectionsPage.tsx · hooks.ts · components/
│   ├── QuotesPage/         index.ts · QuotesPage.tsx · hooks.ts · components/
│   ├── TrashPage/          index.ts · TrashPage.tsx · hooks.ts · components/
│   ├── DuplicatesPage/     index.ts · DuplicatesPage.tsx · hooks.ts · components/
│   ├── SettingsPage/       index.ts · SettingsPage.tsx · hooks.ts · components/
│   ├── StatsPage/          index.ts · StatsPage.tsx · hooks.ts · components/
│   └── errors/             NotFoundPage · ForbiddenPage · ErrorPage + ErrorBoundary
└── main.tsx
```

## The facade

Each page folder has an `index.ts` that is its **only** public surface. `routes.tsx`
imports `from "../pages/Home"`, never from `"../pages/Home/components/BookGrid"`.

The same rule applies downward: nothing outside a `hooks.ts` imports from
`api/generated/endpoints/`. That single indirection is what stops a regeneration rippling
through every component. When the schema changes, the page's hook file absorbs it.

Generated **types** are a different matter and are imported freely, by about fifty files.
`api/generated/model` is a description of the API rather than a call to it: a component
naming the shape it renders is not coupled to how that shape is fetched, and hiding the
types behind a hand-written copy would mean maintaining a second description that can
disagree with the first.

`tests/houseRules.test.ts` enforces the boundary. It held with one exception before that
test existed, a feature-flags call inline in `providers.tsx`, which is now
`useFeatureFlags` in `app/hooks.ts`. One exception is one more than a test can express.

## Dumb components

Components take props and emit callbacks. They do not fetch, and they do not reach into a
query cache. All data work happens in each page's `hooks.ts`, and the page component is
composition only:

```tsx
export default function Home() {
  const library = useLibrary();          // every request for this page
  return <BookGrid books={library.books} onLoadMore={library.loadMore} … />;
}
```

The practical payoff is in the tests: `BookCard` can be rendered with a plain object and no
query client at all.

## The generated API layer

`bun run api:generate` dumps `openapi.json` from FastAPI and runs Orval over it. The output
is **committed**, so a fresh clone can typecheck, test and build with no Python toolchain,
and CI can diff the regenerated result to catch a client that has fallen behind.

Three configuration points in `orval.config.ts` are load-bearing:

- **No top-level `query` block.** Setting one applies to *every* operation, including
  POST/PUT/PATCH/DELETE, which orval then generates as `useQuery` instead of
  `useMutation`. That is not cosmetic: a query runs on mount and on retry, so
  `useDeleteBook(id)` would delete the book as soon as a component rendered. Query options
  are scoped per operation instead.
- **`fetch.includeHttpResponseReturnType: false`.** Orval's built-in fetch client returns a
  `{ data, status, headers }` envelope; our mutator returns the parsed body and throws on
  any non-2xx. Leaving the envelope on would have every generated type describe a shape the
  code never produces.
- **`operations` keys are raw operationIds** (`list_books`), not camelised hook names.

Hook names are clean (`useListBooks`, not `useListBooksApiBooksGet`) because the backend
sets `generate_unique_id_function`. See [decisions.md](decisions.md).

### The mutator

`src/api/mutator.ts` is the single fetch implementation every generated endpoint calls, and
the only module in `src/` that touches `fetch`. It owns:

- attaching the bearer token, and **not** setting `Content-Type` for `FormData` (the
  browser must set it itself to include the multipart boundary);
- ending a stale session on a 401, except from `/auth/login` and `/auth/register`, where a
  401 means *wrong credentials* rather than an expired session;
- flattening FastAPI's three error shapes (a `detail` string, a 422 array of per-field
  objects, or a non-JSON proxy error page) into one displayable message;
- `downloadFile`, for the CSV/TXT export: a plain `<a href>` cannot carry the
  Authorization header, and the filename comes from a response header.

## Localisation

English and German, hand-rolled in `src/i18n/`. No library: two languages and a flat key
set make the whole mechanism an object lookup plus placeholder substitution, and what a
library would add is a dependency, a bundle and a plural-rules engine nothing here needs.

What it does keep is the property that matters. `en.ts` is the source of truth for the key
set and exports `Messages = Record<MessageKey, string>`; `de.ts` is **typed as `Messages`**,
so a key added without a German translation is a compile error rather than an English
sentence appearing mid-paragraph in a German page.

```tsx
const { t, locale, setLocale } = useTranslation();
t("book.by", { author: book.author });   // "by Frank Herbert"
```

`{placeholder}` substitution only. Numbers are formatted with `Intl.NumberFormat` for the
locale; an unsupplied placeholder is left visible, because a literal `{count}` on screen is
obviously a bug where "undefined" reads like a value and blank text reads like nothing is
wrong.

**Choosing a language**, strongest signal first:

1. An explicit choice in Settings, stored per device.
2. The browser's own language, matched on the primary subtag, so `de-AT` counts as German.
3. The server's `default_locale`, which is the admin's answer for a browser set to a
   language this app does not speak.
4. English.

The server default sits *below* the browser deliberately: it answers "what should a stranger
see", not "override what this person's device already says".

Two conventions worth keeping:

- **No em dashes** in any message, in either language. They do not survive translation
  cleanly, they are awkward on the phone keyboards this app is mostly used from, and German
  typography uses them differently. A test asserts it rather than trusting review.
- **Translate whole phrases, never fragments.** `"Loaned to {to} by {by}"` is one key, not
  three pieces concatenated; German does not keep that word order. The same reasoning
  retired `` `By ${category}` `` in StatsPage and `` `No ${active}loans` `` in LoansPage.

## State

Server state is React Query. Client state is `useState`, local to whatever needs it.

The only shared client state is the session, in `pages/hooks.ts`. `readStoredUser()`
tolerates a corrupt `localStorage` value and returns null. A half-written entry would
otherwise throw during the first render and white-screen the app.

Cache invalidation is explicit and lives in the page hooks. A book mutation invalidates
that book, the grid and the loans list, because all three embed book state. Mutations never
retry: a request that may already have created a book must not be re-sent on its own
initiative.

Saving a setting also invalidates `/api/settings/features`, which is a different endpoint
with its own cache entry and drives rendering across the whole app.

**`mutate`, not `mutateAsync`, unless something awaits the result.** `mutateAsync` rejects
on failure, so a fire-and-forget `void save(...)` leaves an unhandled promise rejection in
the console on every failed request. The failure is already carried in the mutation's own
error state, which is what the UI renders.

## Styling

Tailwind 4, configured **in CSS**. There is no `tailwind.config.js` and no
`postcss.config.js`; `@tailwindcss/vite` runs the pipeline and `src/index.css` holds the
theme in an `@theme` block. Following a Tailwind 3 tutorial will have you creating a config
file nothing reads.

Layout is mobile-first. The app bar is fixed to the top and holds three destinations as
icons (library, scan, loans), labelled from `sm` up, with everything else in a menu behind
the account trigger; `NavBar` exports `BAR_HEIGHT` and `BAR_OFFSET` so `App.tsx` clears it
by exactly its height. It replaced a left rail, which spent 56px of a phone's width on
every screen and had nowhere to open a menu into. The book grid uses
`repeat(auto-fill, minmax(170px, 1fr))` and reflows by available width with no breakpoints
at all.

Under `AUTH_MODE=proxy` the menu offers neither sign out nor switch account: the upstream
owns the session, so both are inert and the app re-identifies the same person immediately.

## Errors

Three surfaces, deliberately distinct:

- **A failed request** renders inline through the shared `ErrorState`, usually with a retry.
- **A missing route** renders `NotFoundPage`. This replaced a catch-all that silently
  redirected to `/`, which hid every mistyped or dead link.
- **A render crash** is caught by `ErrorBoundary` and shows `ErrorPage`, the client-side
  500. Without it one component throwing unmounts the whole tree and leaves a blank page.

`ErrorPage` shows the underlying message only in a development build, for the same reason
the backend never returns a traceback.

## PWA

`vite-plugin-pwa` generates the service worker; `registerType: "autoUpdate"` picks up a new
build without prompting. The manifest is hand-maintained in `public/manifest.json`
(`manifest: false` in the plugin config), so icon and name changes go there.

Open Library covers are cached `CacheFirst` for 30 days: immutable content addressed by
ISBN.

The usual service-worker trap applies: the precache holds hashed asset names, so a client
can hold a stale bundle after a deploy. `autoUpdate` resolves it on the next visit; a
client stuck on old assets needs the worker unregistered.

## Three ways to add a book

The scan page offers all three, and they converge on the same confirm step:

| Route | For |
|---|---|
| Barcode | A book physically to hand |
| Typed ISBN | A damaged or unreadable barcode |
| Google Books search | No barcode at all, or adding from a list |

Search is offered only when an admin has configured Google Books, and is **submitted
explicitly rather than debounced**, unlike the library search box. Every search is a billed
call against somebody's quota, and typing "the hobbit" would spend ten of them to answer one
question.

Choosing a result prefills the draft and deliberately leaves the flow's `isbn` state at
null: setting it would restart the ISBN lookup and overwrite the record the reader just
picked with whatever Open Library says.

## Bulk ownership

Home has a selection mode, and it exists for one flow: import a Goodreads library, which
arrives unconfirmed, then tick off the books actually on the shelf. Doing that one book at a
time across a few hundred rows is not a real option.

While selecting, a `BookCard` renders as a **`role="checkbox"` button rather than a link
with its navigation suppressed**. It genuinely is a checkbox at that point, and should be
announced as one rather than as a link that goes nowhere.

"Select all" covers **only the books already loaded into the grid**. The grid pages, and
claiming to select rows the reader has not paged in would send ids they never looked at.

## Rapid intake

The ordinary scan flow is scan, look up, confirm, repeat. That is right for one book and
unusable for three hundred, and cataloguing an existing shelf is the moment most people
abandon a library app. Rapid mode keeps the camera running, looks each hit up in the
background, and asks for one confirmation at the end.

Three properties are load bearing:

- **The same barcode arriving repeatedly is queued once.** The camera fires continuously
  while a barcode is in frame, so without this the queue fills with a single book.
- **A book neither source knew stays in the queue**, as a blank draft. It is still a book
  on the shelf, and dropping it silently is how a catalogue ends up quietly incomplete.
- **Nothing is written until the batch is confirmed.** A scanner that wrote as it went
  would turn every misread barcode into a row somebody has to find and delete later.

The batch is added sequentially, not with `Promise.all`: a 300-book batch would otherwise
open 300 concurrent requests against one SQLite writer.

## Appearance: the palette, light or dark, and the wallpaper

`src/theme/` resolves light and dark exactly the way `src/i18n/` resolves the language:
an explicit choice, then the system's `prefers-color-scheme`, then light. Following the
system by default is the point, and "system" stays a real option rather than the absence
of one, so somebody can go back to following it.

All three preferences live on the **account**, not the device, with a write-through
`localStorage` cache in front of the server so the first paint does not wait on the network.
`AppearanceSync` is the one component that holds both directions. The palettes themselves,
the rule that generated them, the picker and the storage design are in
[theming.md](theming.md).

The picker is `pages/AppearancePage/` at `/settings/appearance`, and it is **inside the
signed-in route table on purpose**. `ThemeProvider` sits above the session gate and does not
unmount on sign-out, so a picker the login screen could reach would write a choice into the
cache of the member who left. That is what `ThemeProvider.release` exists to prevent, and
the route table is the thing that keeps it out of reach.

Three details are load bearing:

- **`@custom-variant dark` in `index.css`.** Tailwind 4's `dark:` follows the media query
  out of the box, which no toggle can override. Redefining it to key off a class on the
  root lets both work, because the class is *set* from the media query when nobody has
  chosen.
- **`applyTheme` runs in `main.tsx`, before React mounts.** Resolving in an effect paints a
  light page and then flips it, which is the flash every dark site is judged by.
- **The media listener is attached only while the preference is `system`.** Otherwise
  somebody who asked for dark gets flipped back at sunrise by their laptop.

`color-scheme` is set alongside the class so the browser's own furniture follows: form
controls, the scrollbar, and the paint before first render.

### The background patterns

`src/theme/patterns.ts` holds ten tileable SVGs, one picked at random per visit: five
William Morris repeats grown along curves, and five decorated papers set out on a lattice.
They are drawn rather than shipped as images. Morris designs are public domain, but the
scans in circulation are mostly museum photographs under their own terms, so "it is a
Morris" is not by itself a licence; and an SVG is a few kilobytes against a few hundred for
a repeating raster, and it takes its colour from the theme instead of needing a second file
for dark.

The choice is stored only if somebody makes one: a null wallpaper means a different one
each time, which is the behaviour this was built around and what a new account starts at.
A device with nobody signed in on it is the exception and is fixed on Willow Bough, because
a front door that changes every visit reads as a slot machine.

Two things about the engine are worth knowing before touching it, and both are measured in
[theming.md](theming.md):

- **A layer states a weight, not an opacity.** The alpha is solved from it against the
  palette's own ink and page, because the same alpha over seven inks is seven different
  weights and the spread was as wide as the whole ink budget.
- **A pattern is admitted by two measurements**, tint contrast and peak coverage, both read
  off the generated tile by `frontend/tests/theme/rasterise.ts`. Adding a pattern that looks
  right and measures wrong fails the suite with the number.

Dark draws more strongly than light, not less, because the tile is mostly negative space: a
pattern that reads as gentle on white disappears into near black at the same weight.

## The wishlist is a saved view

"We want this and do not own it" is already expressible: `want_to_read` plus `not_owned`.
`WISHLIST_FILTERS` in `pages/Home/types.ts` is that pair, and the nav entry is a link
carrying them as query parameters. Adding a fourth reading status for it would put the
same fact in two places and let them disagree.

Home seeds its filters from the URL on mount, which is what makes those links work, and
reads them **once** rather than staying in sync, so clicking a filter afterwards is not
fought by the address bar.

## The barcode scanner

`pages/ScanPage/components/BarcodeScanner.tsx` wraps `@zxing/library` and does two
non-obvious things: it **filters to ISBN shapes** before reporting (Bookland EAN-13 or a
bare 10-digit ISBN), so pointing the camera at any barcode in the room does not fire a
lookup; and it **releases the camera** on unmount and whenever it goes inactive, or the
phone's camera indicator stays lit after navigating away.

The camera requires a **secure context**: HTTPS or `localhost`. On a plain-HTTP LAN address
it fails with a permission error no matter what the member taps.
