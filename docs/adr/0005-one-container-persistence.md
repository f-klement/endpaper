# One container holds the Library

Endpaper is deployed as one self hosted application whose Library data and cover images remain
together. This favours a simple Household installation and backup over independent scaling or a
separate web tier. Changing it requires a migration and an operational redesign, so it is
recorded here.
