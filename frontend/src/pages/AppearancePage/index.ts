/**
 * The appearance picker's public surface.
 *
 * Routes import from here, never from the files inside. The hook is exported
 * for tests; the components are not.
 */

export { default } from "./AppearancePage";
export { usePreviewBooks } from "./hooks";
