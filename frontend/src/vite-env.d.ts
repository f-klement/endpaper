/// <reference types="vite/client" />

/**
 * Substituted at build time by `define` in `vite.config.ts`. Declared rather
 * than imported because it is not a module: there is no value here at runtime,
 * the literal is inlined into the bundle.
 */
declare const __APP_VERSION__: string;
