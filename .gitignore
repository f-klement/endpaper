__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
.mypy_cache/
backend/data/
backend/.venv/
frontend/node_modules/
frontend/dist/
data/
.env
CLAUDE.md
plan.md
# Same reason as plan.md, and load-bearing: it names internal hostnames and
# addresses, which ci/make-public-tree.sh fails the build on.
implementation.md

# Test artefacts
.coverage
htmlcov/
frontend/coverage/

# Output of ci/make-public-tree.sh
/public/

# CI-generated, never committed. Trivy's vulnerability DB is a GitLab cache path and the
# JUnit files are artifacts, but both are written into the project directory, so a local
# `trivy fs .` or `pytest --junitxml` would otherwise show up as untracked noise.
.trivycache/
runtime-requirements.txt
junit-*.xml
trivy-*.json
.uv-cache/

# Competitor research, internal
feature_investi*.md

# Technology evaluation, internal working notes
capacitor_viability.md
